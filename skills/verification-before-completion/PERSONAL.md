## Local workflow mappings

- Scale verification to risk: use no check for prose-only or trivial edits, a targeted check for localized changes, and broader checks for shared or cross-module changes.
- If required evidence is unavailable or a check fails, report the work as incomplete with the evidence available, without moral judgment.
- Use `delegating-subagents` before deciding whether to delegate or parallelize agent work.

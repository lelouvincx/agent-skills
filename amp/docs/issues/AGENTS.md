# Amp issue documentation rules

- Before creating or editing an issue document, read `_schema.md`; it is the sole contract for frontmatter, headings, identifiers, and filenames.
- Preserve original intent and incident evidence. Update resolution status without rewriting history as if resolved behavior existed at the time.
- Link every issue from `README.md`, and link affected capability documents back to the issue when it explains their contract.

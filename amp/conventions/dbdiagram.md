# dbdiagram conventions

- Check for and use the official `dbdiagram` CLI as the unified CLI for dbx, dbdiagram, dbdocs, and runsql work.
- Use `dbdiagram build document` for dbdocs publishing workflows.
- Treat the standalone `dbdocs` CLI as legacy.
